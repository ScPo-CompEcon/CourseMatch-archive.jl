function while2()
      global ptild = Array[DoubleN[1]]
      global DoubleN = DoubleN[2:end]
      global dem = d(ptild)
      # if from l.13 to 15
      if (dem in τ) == false #If dem is not in the tabu list
          println("Then this line twice")
          global foundnextstep = true
      else
          println("This line first")
      end
  end
